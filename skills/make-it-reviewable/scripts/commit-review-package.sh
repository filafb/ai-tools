#!/usr/bin/env bash
# commit-review-package.sh <base> <head> [out-file]
# Emits, to one file, the commit list + per-commit diff -U10 + stat for
# base..head, and prints the file path. Stub — not implemented yet.
set -euo pipefail
exit 0
